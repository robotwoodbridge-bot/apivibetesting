meta {
  name: get-cart
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/cart
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{authToken}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
