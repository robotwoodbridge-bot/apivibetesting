meta {
  name: login-missing-password
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "demo@techshop.com"
  }
}

tests {
  test("Status code is 400 (missing password)", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
