meta {
  name: login-missing-email
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "password": "password123"
  }
}

tests {
  test("Status code is 400 (missing email)", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
